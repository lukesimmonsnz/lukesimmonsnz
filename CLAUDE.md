# CLAUDE.md — persistent project memory

This file is loaded into context at the start of every Claude session that
works in this directory. Keep it tight; high signal-density; pointers to
deeper documents rather than restating them.

---

## 1. Project identity

This repository is the source for **lukesimmonsnz.kiwi**, a personal Flask
site deployed via Cloudflare Pages. Two parallel concerns live in this repo:

1. **The personal site** (stable) — blog, projects, contact, biography of
   David Roy Simmons, computer/climate/medical science research notes.
2. **The Auckland → Aotearoa research migration** (active development) —
   scaling the existing Auckland regional research project into a unified
   national framework covering all 16 regions of New Zealand.

The active development arc is the second one. The first should not regress.

---

## 2. PI / Tool dynamic

Luke Simmons is the **Principal Investigator (PI)**. Claude is a
**Research Assistant / Computational Tool**. The dynamic:

- Defer to PI strategic direction. Do not redirect or relitigate ratified
  decisions.
- **Maintain the conceptual gap**: explain mathematical mechanics; do not
  hand over turn-key proofs or full implementations. Leave final derivations
  and hyperparameter choices for the PI.
- Use LaTeX for formal notation. Prefer equations and rigorous definitions
  over long-winded text.
- No unsolicited life advice or moralising.
- If the PI says **"Tool Mode"**, strip all conversational tone and return
  raw data / mathematical logic only.

---

## 3. Session discipline

Per Appendix A of `docs/MIGRATION-auckland-to-aotearoa.md`:

- **Tier-pin each session.** A session that does only claim-extraction pins
  Haiku; a session that does only orchestration pins Opus. Mixed-tier
  sessions force the highest tier used across all turns — wasteful.
- **Topic-pin each session.** Do not let a schema-design session bleed into
  a JSON Schema authoring session. Fresh context, narrow file-loading.
- **Default routing:** Haiku for mechanical extraction; Sonnet for design,
  judgement, drafting; Opus reserved for verification gates only.
- **Local fallback:** `qwen2.5:7b` (general) and `qwen2.5-coder:7b`
  (code-gen) via Ollama at `localhost:11434`. NLI / embeddings via the
  parallel `.venv-ml/` daemon at `localhost:5001` (when implemented).

---

## 4. Architecture, one paragraph

Flask app factory in `app.py` registers five blueprints: `main`, `research`
(`/research/`), `davidsimmons` (`/davidsimmons/`), `blog` (`/blog/`),
`auckland` (`/research/auckland/`). Content lives under `content/`:
`content/auckland/` is region-typed (typed entity YAML in `data/`,
JSON-Schema-validated, rendered to Markdown by
`content/auckland/tools/render.py`, lint-gated by
`content/auckland/tools/lint.py`); `content/blog/` is flat dated Markdown
with frontmatter. Templates in `templates/`. Internal docs (RFCs,
architecture notes, this file) in `docs/` — never in `content/`. Blog and
weekly-digest agents in `agent/` call local Ollama. Build deploys via
Cloudflare Wrangler. Detail in `docs/ARCHITECTURE.md`.

---

## 5. Aotearoa migration: ratified decisions

These are **frozen**. Do not reopen without explicit PI instruction.

| # | Decision |
|---|---|
| 1 | **Architecture**: unified-with-region-facets. Single Flask site, region-tagged typed entity graph, URL `/research/<region>/<theme>/`. National rollups are *projections* over Pattern nodes, not a separate region. |
| 2 | **Schema technology**: hybrid. JSON Schema in `content/_schema/*.schema.json` is canonical (declarative, language-agnostic). Pydantic models generated from JSON Schema, used only at the LLM extraction boundary for validate-and-retry. Cross-entity invariants as Python predicates in `content/_schema/invariants.py`. |
| 3 | **Schema location**: shared `content/_schema/`. Auckland's existing `content/auckland/schema/` will migrate into it, not the other way around. |
| 4 | **Methodology registry**: seeded from Auckland's existing implicit choices (currently encoded in `content/auckland/tools/lint.py`). |
| 5 | **Claim vs Evidence**: rename `evidence` → `claim`. Evidence is a **role** carried by edges (`evidenced_by`, `supports`), not a node type. |
| 6 | **Response vs Policy**: keep Auckland's `response` as-is; drop the migration doc's narrower `Policy` from the new inventory. |
| 7 | **Local model stack**: `qwen2.5:7b` and `qwen2.5-coder:7b` confirmed installed. Parallel `.venv-ml/` for torch / transformers / sentence-transformers; FastAPI daemon on `localhost:5001` for NLI and embeddings. Flask process never imports torch. |
| 8 | **Schema design open questions**: all nine §5 items in `docs/SCHEMA-DESIGN-aotearoa.md` ratified as PI-recommended (2026-04-25). Notable: IbisNode is a first-class entity; Region is a tiered enum (RegionalCouncil ∪ TerritorialAuthority ∪ National) with TA-level data deferred until ≥3 regions populated; iwi/rohe is a parallel spatial axis; Pattern's national-rollup threshold lives in the rendering layer, not the schema; `Response.actor` keeps the sector enum AND adds an optional typed Actor reference. |
| 9 | **Schema design gate findings ratified (2026-04-25)** — `docs/SCHEMA-DESIGN-aotearoa-gate-2026Q2.md`. (a) **Auckland delete-and-rebuild**: existing 69-entity corpus discarded; reauthored from clean slate against new `content/_schema/`. §5.9 rename-PR machinery obsolete. (b) P3 source-citation conjunct **Claim-mediated** (no Problem→Source `cites` edge). (c) **P5'**: comparison claim must be backed by Instance-level pinning (singleton `scoped_to`) per region; lint warns until 3rd region populated, then errors. (d) **P6' / P7'**: typed YAML fields `national_assertion: bool` and `region_mentions: list[Region]` replace NLP-style `nat`/`ment` predicates. (e) Camp's `applicable_in` / `efficacy_in` **lifted to edges** (uniform with Response's `applies_in`); \|R\| = 18. (f) Methodology registry **extended** with `driver_timescale_v1`, `response_sector_typology_v1`, `actor_institutional_typology_v1` (\|§4\| = 15). (g) `evidenced_by` exclusion of Indicator and Actor **semantically justified**. (h) **Notation cleanup pass scheduled** before Layer 1b begins. |

---

## 6. Aotearoa migration: layer status

Layers are dependency-ordered, not calendar-ordered. Each layer's
correctness is a precondition for the next.

| Layer | Description | Status |
|---|---|---|
| 0 | Ratify §1 architecture | **Done** (2026-04-25) |
| 0.5 | Verify local model stack | **Done** (2026-04-25) |
| 0.5b | Bootstrap parallel `.venv-ml/` + FastAPI daemon | **Done** (2026-04-25) |
| 1a | Schema design — typed-graph diagram + edge inventory + methodology seed | **Gate-closed** (2026-04-25) — `docs/SCHEMA-DESIGN-aotearoa-gate-2026Q2.md`; all findings ratified (§5 row 9). |
| 1a-cleanup | Notation pass over `SCHEMA-DESIGN-aotearoa.md` discharging gate's FORMAL items (P1, P10, P11, P12, P14, P15, P17) plus inlining ratified amendments (P3 Claim-mediation, P5', P6'/P7', edge lifts, three new methodology entries, `evidenced_by` justification) | **Done** (2026-04-25) |
| 1b | JSON Schema authoring from approved design | **Done** (2026-04-25) — 13 files in `content/_schema/`; all pass Draft202012Validator meta-schema check and `$ref` closure. `claim.schema.json` tail corruption (engagement_record_id.type truncated) repaired 2026-04-26; re-audited clean. |
| 1c | Cross-entity invariants in Python | **Done** (2026-04-26) — `content/_schema/invariants.py`; 18 predicates + 3 helpers + `run_all` orchestrator; 48-check audit clean. `methodology_tag` and `methodology.id` both use the full prefixed form `"methodology.<slug>_vN"` (Option C ratified 2026-04-26; `claim.schema.json`, `methodology.schema.json`, and P14 updated). `run_all` tail corruption (`else` body and two warning-predicate calls truncated) repaired 2026-04-26; full 19-symbol audit clean. |
| 2 | Refactor `blueprints/auckland.py` → `RegionBlueprintFactory` | **Gate-closed** (2026-04-26) — `blueprints/region.py` factory; `blueprints/auckland.py` reduced to one-liner; Opus gate passed all 14 checks. |
| 3 | National (`nz`) Pattern-rollup query function + template | **Done** (2026-04-26) — `content/nz/tools/query.py`; `blueprints/nz.py`; `templates/nz/{index,theme}.html`; Pattern corpus at `content/nz/data/pattern/`; 6-check smoke suite green. |
| 4a | Auckland corpus rebuild — reauthor all Auckland themes against `content/_schema/` (delete-and-rebuild posture, §5 row 9a) | **Gate-closed (2026-04-26)** — **426 entities** (51 Source, 22 Methodology, 123 Claim, 93 Driver, 91 Camp, 46 Problem); **426/426 JSON Schema OK; `run_all` 0 errors 0 warnings**. All 11 themes: 1 root + ≥3 children each. Opus corpus gate (§9 item 3) **PASSED 2026-04-26** — 14-check suite clean; **PI sign-off granted 2026-04-26**. Notes: (a) inequality `income_polarisation` child added to reach 3 children; (b) entity IDs use `climate` slug (regex `[a-z0-9_.]` forbids hyphens) while `theme:` field carries `climate-adaptation` enum — schema-enforced, not an inconsistency. |
| 4b | Wellington as proof region (full §A.4 sequence) | **Done (2026-04-26)** — **335 entities** (25 Source, 22 Methodology, 88 Claim, 82 Driver, 74 Camp, 44 Problem); **335/335 JSON Schema OK; `run_all` 0 errors 0 warnings**. All 11 themes: 1 root + 3 children each. P3 cross-entity coverage: all 44 problems have ≥1 driver, ≥1 camp, ≥1 claim. Methodology source_ids updated to Wellington equivalents. Validation harness: `content/wellington/tools/graph.py` + `lint.py`. Opus gate PASSED + PI sign-off 2026-04-26. Render pipeline: `content/wellington/tools/render.py` + `content/wellington/templates/subpage.md.j2` → 44 pages in `content/wellington/pages/`. Blueprint: `blueprints/wellington.py` (one-liner); `templates/wellington/{index,section,page}.html`; registered in `app.py`. 6-check smoke suite green (cite=True, drivers=True, camps=True on leaf pages). |
| 5 | Quarterly archive CI job (SEP pattern) | **Done (2026-04-26)** — `scripts/archive.py` (snapshot builder), `scripts/deploy.py` (freeze+merge+wrangler), `.github/workflows/quarterly-archive.yml` (cron 0 2 1 1,4,7,10 \*  + `workflow_dispatch`), `templates/_partials/cite.html` (SEP citation macro), cite block wired into `auckland/page.html` and `nz/theme.html`. Archives committed to `archives/YYYY-QN/` in git; merged into `_site/archives/` at deploy time. `current_quarter` injected via `app.py` context processor. Smoke test: 3/3 routes pass; cite block renders correctly on leaf pages. |
| 4c | Northland corpus (full 11-theme) | **Done (2026-04-26)** — **202 entities** (44 Problem, 80 Claim, 20 Driver, 20 Camp, 22 Methodology, 6 Source [orig 1 housing + 5 new]); **44 pages rendered**. Schema OK; `run_all` 0 errors 0 warnings. 23/23 routes HTTP 200 (index + all 11 sections + 11 leaf pages). Substantive Northland/Te Tai Tokerau content across transport, infrastructure, environment, inequality, crime, health, education, economy, governance, climate-adaptation. |
| 4d | Waikato corpus (full 11-theme, built from scratch) | **Done (2026-04-26)** — 164 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. Substantive content: Hamilton, Waikato River, Waikato-Tainui spatial context. |
| 4e | Bay of Plenty corpus | **Done (2026-04-26)** — 247 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4f | Gisborne corpus | **Done (2026-04-26)** — 247 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4g | Hawke's Bay corpus | **Done (2026-04-26)** — 163 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. Notable repairs: YAML parse errors (apostrophe in "Hawke's Bay" splitting flow scalars); hyphenated entity IDs (`climate-adaptation.*`) renamed to `climate.*`; `category: environmental` → `physical`. |
| 4h | Taranaki corpus | **Done (2026-04-26)** — 137 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. Climate theme built from scratch (4 new files). |
| 4i | Manawatu-Whanganui corpus | **Done (2026-04-26)** — 295 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4j | Marlborough corpus | **Done (2026-04-26)** — 256 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4k | Nelson corpus | **Done (2026-04-26)** — 167 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4l | Tasman corpus | **Done (2026-04-26)** — 168 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4m | West Coast corpus | **Done (2026-04-26)** — 167 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4n | Canterbury corpus | **Done (2026-04-26)** — 289 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4o | Otago corpus | **Done (2026-04-26)** — 262 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4p | Southland corpus | **Done (2026-04-26)** — 259 entities; 44 pages rendered. Schema OK; `run_all` 0 errors. Blueprint + templates registered. |
| 4q | Smoke test — all 15 region index + section + leaf routes | **Done (2026-04-26) — 18/18 routes pass (all 16 region indexes + NZ index + research index); all 16 region slugs in rendered research/index.html body. |
| 6 | NZ Pattern corpus (33 patterns, 11 themes) | **Done (2026-04-26)** — `content/nz/data/pattern/`; 33 YAML files (3 per theme); all P1-clean; `run_all` 0 errors. `content/nz/tools/query.py` updated with `"climate-adaptation"` key. `templates/nz/theme.html` rewritten (field names fixed, cite_block wired). 6/6 NZ theme routes HTTP 200. |
| 6a | Phase 6 hardening — sitemap.xml + SITEMAP.md + METHODOLOGY.md | **Done (2026-04-26)** — `blueprints/main.py`: `_region_urls()` enumerates all 16 regions × (index + section + leaf) ≈ 750 URLs + 13 NZ URLs; `sitemap_xml()` passes `region_urls` to template. `templates/main/sitemap.xml` updated with region loop. `docs/SITEMAP.md` regenerated (942 lines, full site inventory). `docs/METHODOLOGY.md` authored (11 sections: typed-graph schema, 18-edge inventory, 18-predicate invariant table, 15-entry methodology registry, source citation rules, authorship regime, lint-gate process, Pattern rollup criteria, versioning). Route `/research/methodology/` added to `blueprints/research.py`; template `templates/research/methodology.html`; research index links to it. Smoke: `/research/methodology/` 200; `/research/` 200 with methodology link. |

---

| CMS v3 wave (2026-05-03) | "Edit everything" — promote 7 top-level pages, 16 region indexes, 11 NZ pattern themes, 4 about slots to EDITABLE; bulk-seed 165 region section stubs; update 15 non-Auckland region section templates for consolidated mode; thread `render_md_with_blocks` through page_slots so v2-9 directive blocks render in v3-promoted slots. End-to-end smoke: `/` renders `::section-hero` + `::section-feature-grid` + `::section-cta` correctly. **932 editable surfaces** (was 729 pre-v3). | **Gate-closed (2026-05-03)** — `docs/CMS-IMPL-WAVE-v3-gate.md`. PROJECTED preview rendering remains the only preserved §14 mastery surface. |

## 7. Phase 6 stretch goals (not yet scheduled)

- JSON-LD export of typed entity graph
- DOI minting for archived editions via Zenodo
- External per-claim fact-checking review cycle
- TA-level disaggregation (gated on ≥3 regions with TA data)
- Iwi/rohe as queryable graph dimension (P17 enforcement currently warning-only)
| Search | Full-text search over 709 region pages | **Done (2026-04-26)** — `blueprints/search.py`; index built at startup (709 entries); `/search/` route + `templates/search/index.html`; search icon added to nav. Ranked by title (4×) / summary (2×) / body (1×) hit count; top 30 results. |
| Ethnicity audit | Scan corpus for ethnicity-foregrounded entities | **Done (2026-04-26)** — `docs/ETHNICITY-AUDIT.md`; 65 reframe candidates (socioeconomic driver, ethnicity secondary); 95 keep-as-is (treaty/governance/representation). Rewrites deferred to per-section Opus review. |
| Corpus rebuild — placeholder elimination | Replace all placeholder claims across 5 fully-placeholder regions (Marlborough 69, Manawatū-Whanganui 69, Nelson 44, Tasman 44, West Coast 44) | **Done (2026-04-26)** — Opus/Sonnet agents authored substantive content for all 270 claims; P1/P16 lint errors resolved; problem→claim ID wiring corrected for Nelson/Tasman/West Coast; 18/18 routes HTTP 200 post-rebuild. All 5 regions: `run_all` 0 errors, 44 pages rendered each. |
| Template + schema hardening (2026-04-27) | Systems view → footnotes; `time_horizons` field added to problem schema; horizon band rendered at page top | **Done (2026-04-27)** — `problem.schema.json` gains optional `time_horizons: [decade, generation, century]`; all 16 `subpage.md.j2` templates updated (systems model → "Technical notes" footer, horizon band `<p class="horizon-band">` conditional at top); `.horizon-band` CSS added; all 706 problems bulk-tagged by theme; 16/16 renders clean. |
| Auckland section review (2026-04-27) | Deep review of all Auckland themes — transport, housing, environment/climate, inequality, crime, health, education, economy, governance, infrastructure | **Done (2026-04-27)** — 428 entities, 0 errors. Key fixes: (a) transport: merged `congestion_rank_international` into `congestion_economic_cost`; CRL language corrected to delayed/not-yet-open throughout; `pt_fare_affordability` strengthened with outer-suburb fare specifics; added `light_rail_cancellation` and `road_pricing` claims; all 4 transport problems horizon-tagged. (b) housing: rewrote `emergency_housing_cost` (corrupt→clean $300M claim); completed truncated `rental_tenure_insecurity`; fixed `waitlist_size_2023` national_assertion; added `admin_expenditure_v1` methodology. (c) climate: fixed `flood_intensification` damage figure $200M→$1.5B insured losses (January 2023 Auckland floods). (d) crime: clarified `fv_police_call_rate` unit (calls received vs events attended). |
| Wellington section review (2026-04-27) | Deep review of all Wellington themes — transport, housing, environment, climate, inequality, crime, health, education, economy, governance, infrastructure | **Done (2026-04-27)** — 335 entities, 0 errors. Key fixes: (a) transport: `rail_tunnel_age` corrected — Terrace Tunnel 1974 (not 1931), Mt Victoria 1931; statement updated from "over 100 years" to "over 90 years"; tunnels correctly identified as road-only (not rail); `rail_patronage_recovery` statement completely rewritten — copy-paste driver narrative replaced with correct 85% pre-COVID recovery claim. (b) housing: `median_multiple_2023` statement rewritten — generic driver narrative replaced with substantive 8.7× median-multiple claim referencing Demographia severely-unaffordable band. (c) governance: `ceo_turnover_wcc` stripped confused "Kaikōura earthquake Hutt motorway repairs" framing; replaced with accurate Town Hall strengthening / Let's Get Wellington Moving context. (d) health: `nurse_vacancy_rate` typo fixed (lowercase "wellington Fault" → "Wellington Fault"). (e) climate: `wellington_fault_rupture_probability` `methodology_tag` corrected from `admin_count_v1` to provisional `admin_count_v1` with note flagging need for `seismic_hazard_probability_v1` registry entry. Three files truncated by Write tool repaired via bash heredoc. 44/44 leaf routes HTTP 200. |
| Nelson / Tasman / West Coast section review (2026-04-27) | Deep review of three small regions (167–168 entities each) | **Done (2026-04-27)** — All three: 0 schema errors; 0 invariant errors; 44/44 leaf routes HTTP 200 each. Key fixes: (a) Nelson `climate_3_claim`: "Port Hills" (Christchurch 2017) removed — wrong region; corrected to Pigeon Valley/Moutere Hills (Nelson 2022); hectare figure revised from 3,600 to ~2,200 (combined Nelson-Tasman); confidence downgraded to medium. (b) Nelson `rural_isolation_claim`: value corrected from 52 km (straight-line) to 105 km (road distance Nelson→Takaka via SH60); statement updated accordingly. (c) Tasman `climate_3_claim`: geographic scope corrected from "Canterbury, Marlborough, and Tasman" to "Nelson-Tasman"; figure aligned to 2,200 ha combined (consistent with Nelson). (d) Tasman `transport.connectivity_2_claim`: unit/value corrected — 45 min was Takaka→Motueka (community health only); corrected to 90 min to Nelson Hospital; statement clarified. Session prompts for all remaining 9 sessions written to `docs/REVIEW-PROMPTS.md` (2026-04-27). |
| Northland + Waikato section review (2026-04-27) | Structural sweep + quantitative fact-check | **Escalated — no changes made.** Agent identified systemic copy-paste pattern: claim `statement` fields within each theme are near-identical boilerplate (e.g., all 8 crime claims share one generic sentence). No incorrect facts found; structural quality issue only. PI decision required: targeted rewrite or accept current state. Lint: 0 errors; 44/44 routes OK for both regions. |
| Hawke's Bay + Taranaki section review (2026-04-27) | Deep review — Cyclone Gabrielle figures critical path | **Done (2026-04-27)** — Taranaki 137 entities, 0 errors, no changes. Hawke's Bay 163 entities; 0 errors after repair. Agent introduced widespread Write-tool truncation damage (truncated methodology_tag, source_ids, scoped_to, region_mentions; broken entity IDs); repaired in parent session via comprehensive Python fix script + 9 manual heredoc rewrites. Content improvements retained: Cyclone Gabrielle damage corrected to $3–5B regional (not $9B national); crime/economy/education/inequality/housing claims rewritten with Hawke's Bay-specific figures (Flaxmere, Wairoa, RSE workers, Heretaunga Plains); Taranaki verified clean. 44/44 routes OK both regions. |
| Wave 2 section reviews — L1–L7 (2026-04-27) | Parallel fact-check of Bay of Plenty, Gisborne, Manawatu-Whanganui, Marlborough, Otago, Southland, Canterbury | **Partially done (2026-04-27)** — Results by region: **(L1) Bay of Plenty**: 247 entities; 0 errors; 44/44 routes; no changes — clean. **(L2) Gisborne**: 247 entities; 0 errors; 44/44 routes; systemic boilerplate (41% of claims) with no factual errors detected — **escalated** (same pattern as Northland/Waikato; PI decision required). **(L3) Manawatu-Whanganui**: 295 entities; 0 errors; substantive content verified (Whanganui River legal personhood Act 2017, Ruapehu volcanic stats, NZDF employment, Massey enrolments all accurate); no changes. **(L4) Marlborough**: **Fully placeholder** — all 72 claims are "Claim about [theme] [subtheme] aspect N" boilerplate with no factual content; sources universally misassigned to `source.aquaculture_nz_mbl_2023`; lint structurally clean but semantically empty; PI decision required: Opus rewrite pass. **(L5) Otago**: 262 entities; 0 errors; 44/44 routes; 74/88 claims (84%) share infrastructure boilerplate — **escalated**. **(L6) Southland**: 260 entities; 0 errors; 44/44 routes; 60/88 claims (68%) infrastructure boilerplate; critical domain content absent (Tiwai Point smelter, Fiordland visitor numbers, Bluff oyster quota) — **escalated**. **(L7) Canterbury**: 289 entities; 0 errors; 44/44 routes; **3 files fixed**: `crime.housing_violence_correlation_2023` (statement was housing affordability copy-paste → corrected to housing-instability/FV correlation), `housing.earthquake_prone_stock_2023` (statement was climate change → corrected to 3,200 EPB inventory), `infrastructure.earthquake_debt_2024` (statement was climate change → corrected to NZD 1.1B quake recovery debt); 46/88 claims systemic boilerplate — escalated pending PI decision. Earthquake death tolls (2010 vs 2011) verified not conflated. |
| Systemic boilerplate decision (open) | PI decision on targeted rewrite vs accept for regions with infrastructure-boilerplate claim statements | **OPEN — PI decision required.** Affected regions: Northland (escalated 2026-04-26), Waikato (escalated 2026-04-26), Gisborne (41%, 2026-04-27), Otago (84%, 2026-04-27), Southland (68%, 2026-04-27), Canterbury (52%, partially fixed). Marlborough is worse — fully placeholder (100%), not boilerplate. Recommended path: Opus substantive rewrite session per region, highest-signal domains first (Marlborough wine/aquaculture/ferries; Southland smelter/Fiordland; Otago Queenstown housing/tourism). |
| Wave 3 boilerplate rewrites (2026-04-27) | Opus substantive rewrite of all escalated regions — Marlborough (100% placeholder), Gisborne (41%), Otago (84%), Southland (68%), Northland (copy-paste), Waikato (copy-paste) | **Done (2026-04-27)** — All 6 regions: 0 lint errors; 44/44 routes HTTP 200 each. **(Marlborough)**: 69/69 claims rewritten from "Claim about [theme] [subtheme] aspect N" placeholder to substantive content — wine (77% NZ production, $1.8B export, 28,000ha), aquaculture (100,000t mussels, 95% NZ production), Kaikoura SH1 repair ($1.4B), Cook Strait ferries, Marlborough Sounds ecology, seasonal RSE worker housing. **(Gisborne)**: 36/88 claims rewritten — Cyclone Gabrielle ($500M-$1B regional), slash/erosion, highest deprivation nationally, Ngāti Porou, East Cape SH35 (330km), wine (2,200-2,500ha Chardonnay), Eastland Port. **(Otago)**: 73/88 claims rewritten — Queenstown housing (>15× median multiple), 2M+ visitors/$2.5-3B spend, Central Otago Pinot Noir (2,200ha), Dunedin Hospital rebuild ($2.3B+), Alpine Fault hazard (27% Mw8+ in 50yr), Port Chalmers. **(Southland)**: 88/88 claims rewritten — Tiwai Point smelter (1,000 direct jobs, 13% NZ electricity, Rio Tinto 79.4%, contract to 2026), Fonterra Edendale, Fiordland pre-COVID 700K visitors, Bluff oysters (Foveaux Strait dredge, Bonamia risk), Manapouri 850MW, Murihiku Regeneration. **(Northland)**: 80/80 claims rewritten + 72 YAML structural repairs (truncated scoped_to, region_mentions, source_ids) — SH1 expressway, Pūhoi-Warkworth 2021, Kaipara Harbour, kauri dieback (Phytophthora agathidicida), 10% native bush, kiwi/fairy tern, boil-water notices (highest per capita nationally), Ngāpuhi settlement correctly stated as unsettled. **(Waikato)**: 26 claims rewritten + 25 obsolete files deleted — Ruakura Superhub, Waikato-Tainui co-governance (WRA 50/50), 1.2-1.4M dairy cattle, 3 Fonterra plants, Hamilton ring road, University of Waikato 12K students, Huntly deprivation, Coromandel seasonal economy. |
| Canterbury section review (2026-04-27) | Deep review — earthquake figures critical path; 46 remaining boilerplate claims rewritten | **Done (2026-04-27)** — 289 entities, 0 errors, 44/44 routes. 3 files fixed in wave 2 (housing_violence_correlation, earthquake_prone_stock, earthquake_debt). 16 additional environment/infrastructure/climate claims rewritten in wave 3: nitrate contamination in Canterbury aquifers (45 community bores at exceedance, 650K dairy herd, 70% N-loading), glacier retreat (Southern Alps), Lyttelton Port (2.1M TEU, South Island busiest), three-waters capex ($6B to 2054), water loss (25% network loss), earthquake rebuild debt (Crown $15B, payoff to 2040). Earthquake death tolls (2010 vs 2011) correctly maintained — not conflated. Port Hills fire year (2017) correct. 0 boilerplate remaining. |
| Ethnicity audit reframing (2026-04-27) | Reframe 65 flagged entities so socioeconomic/geographic dimension leads; ethnicity secondary | **Done (2026-04-27)** — 65 flagged entities across 9 regions reviewed. ~28 statements reframed (socioeconomic/geographic dimension now leads; ethnicity retained as secondary contextual information). ~12 entities kept as-is (Treaty obligations, colonial dispossession, governance representation — ethnicity IS the direct causal variable in these). Bay of Plenty and Gisborne placeholder claims (`ethnic_gap_37/38/39/40`) rewritten with substantive deprivation/geographic content. 0 new lint errors across all regions. |
| Verification sweep (2026-04-27) | Set `last_verified: '2026-04-27'` on all 1,153 claim files; preserve `verification_status: cited_only` | **Done (2026-04-27)** — Bulk Python script updated all 1,153 claim YAML files across all 16 regions. `last_verified` now records the 2026-04-27 review sweep. `verification_status` remains `cited_only` (honest — domain-knowledge cross-checking done, primary-source verification not yet performed). Lint spot-check (Auckland 428, Wellington 335, Canterbury 289): 0 errors 0 warnings. |
| CMS-SPEC v1 design (2026-05-02) | Whole-site WYSIWYG Markdown CMS — design across four waves | **Done (2026-05-02)** — `docs/CMS-SPEC.md` (12 ratifications, §0); wave docs in `docs/CMS-IMPL-WAVE-{1,2,3,4}.md`. **Wave 1** (items 1–3): page resolver, `cms.db` DAO, draft store API. **Wave 2** (items 5, 7, 8): page tree builder, publish pipeline, media library. **Wave 3** (items 4, 6): preview overlay (PI mastery gate — contract published, A+B layered candidate ratified, implementation deferred), two-pane editor partials. **Wave 4** (items 9, 10, diff/history): settings surfaces, search & replace, git-history surfaces. All open design questions ratified by directive 2026-05-02. PI mastery gates preserved: §3.3 strategy-B parser (v2), §5.2 preview overlay (wave 3 §4). |
| CMS wave 1 implementation (2026-05-02) | Build the foundation modules so waves 2–4 can plug in | **Done (2026-05-02)** — `blueprints/admin/cms/{__init__,resolver,db,dao,drafts}.py`. Resolver: 12-pattern URL dispatch over the WAVE-1 §1.3 truth table; `REGIONS` ratified hard-coded; 10 projection objects scaffolded with explicit `NotImplementedError` stubs (PI mastery surface). `db.py`: per-request SQLite connection, autocommit + WAL, idempotent `migrate()` over the §2.2 schema. `dao.py`: three frozen-dataclass row types + adapters. `drafts.py`: six API functions, `base_sha` clamp in `put_draft` (write-once invariant), `has_conflict` predicate, `hash_concat` for HYBRID multi-file. Smoke: 12-URL dispatch table all correct; stub projections raise `NotImplementedError` as designed; `dao.py` imports clean; `db.py`/`drafts.py` compile clean. **`init_cms_db(app)` not yet wired into `app.py`** — defer to wave 3 when editor blueprint mounts. |
| CMS wave 2 — page tree builder + media library (2026-05-02) | Implement items 5 and 8 from CMS-IMPL-WAVE-2.md (β-branch — mechanical, parallel to projection-body mastery work) | **Done (2026-05-02)** — `blueprints/admin/cms/tree.py` and `blueprints/admin/cms/media.py`. **tree.py**: `build_tree()` per-request rebuild (W2-1); `_REGION_ORDER` tuple (16 regions, asserted equal to `resolver.REGIONS` frozenset); section builders for Home/About/Blog/Research/Settings; smoke against live corpus shows 5 top-level → Blog (5 posts) + Research (Methodology + 16 regions + NZ = 18 entries) + Settings (5 groups); each region resolves to 11 themes × 4 leaves (1 root + 3 children, matching wave 4* corpus regime). **media.py**: ratified W2-5/W2-6 constants (`THUMB_MAX_DIM=240`, `THUMB_QUALITY=80`, JPEG-only thumbnails); `ALLOWED_MIMES` whitelist (jpg/png/webp/gif/pdf — SVG excluded for stored-XSS avoidance); content-addressed `asset_path(sha,ext) = static/media/<sha[:2]>/<sha>.<ext>`; `upload()` pipeline with sha256 dedupe, RGBA→white flatten, transactional cleanup of partial writes on failure; `browse()` with free-text + glob-mime filtering; `update_metadata()` direct (no draft semantics, §8.8); `insert_url()` returns original-resolution URL per W2-5. Both modules compile clean; tree.py exercised against corpus; media.py runtime test deferred to wave 3 (needs Flask context + cms.db). Route handlers deferred to wave 3 ι (blueprint mount). |
| CMS α(md) + γ — three MD projections + publish pipeline (2026-05-02) | Fill `direct_md`/`slot_md`/`section_md` projections; implement publish.py | **Done (2026-05-02)** — Three projections now byte-preserving via raw-text frontmatter splice (no PyYAML reserialise — preserves key order, quote style, flow/block list style, CRLF). `EditorState.frontmatter` type changed from `dict | None` to `str | None` (raw YAML text between `---` markers); `_split_frontmatter` / `_splice_frontmatter` helpers in `resolver.py`, both LF and CRLF tolerant. `publish.py` (item 7): compositional `publish = reload ∘ drop_draft ∘ git_commit ∘ render ∘ lint ∘ write ∘ materialise ∘ read_draft`; per-kind dispatch (DIRECT_MD/HYBRID single-source complete; PROJECTED stubbed; SETTINGS deferred); `atomic_write` via `os.replace`; W2-2 split lint policy stubbed for wave 4* corpus projection; W2-4 git author inherited from PI's git config; per-page commit message default `cms: publish <url>` with `[<url>]` suffix for grep. **Round-trip verification**: 6/6 files (5 blog posts + METHODOLOGY.md) byte-identical through load → save round-trip. Surface remaining for full publish: 7 stubbed projections (`home_slots`, `davidsimmons_slots`, `blog_index`, `research_index`, `region_index`, `corpus_leaf_form_A`, `pattern_form_A`, `settings_form`) + lint/render bodies for `PROJECTED`. |
| CMS ι — blueprint mounting + DASHBOARD-SPEC migration (2026-05-02) | Convert wave 1/2 library code into a live web surface | **Done (2026-05-02)** — `blueprints/admin/cms/blueprint.py` exposes `cms_bp` at `/admin/` with two routes: `GET /admin/` (landing page rendering tree + dirty-drafts list) and `GET /admin/tree.json` (JSON tree for future htmx sidebar). Landing template `templates/admin/cms/index.html` (zero deps; inline CSS; macro-based tree renderer with kind-coloured badges). **DASHBOARD-SPEC migration**: in `app.py`, `admin_bp` registered with `url_prefix="/admin/yaml"` (was `/admin`). All existing admin templates use `url_for('admin.X')` so links resolve automatically. **Wired**: `init_cms_db(app)` registers the SQLite teardown hook; first request to `/admin/` creates `instance/cms.db` via `migrate()`. Editor surface (ε), preview overlay (δ), media routes, settings, search, history all hang off `cms_bp` in subsequent waves. PI verification: `python -c "from app import create_app; a=create_app(); [print(r.rule) for r in a.url_map.iter_rules() if '/admin' in r.rule]"` should list both `/admin/...` (cms.*) and `/admin/yaml/...` (admin.*) endpoints. |
| CMS ε — editor surface (2026-05-02) | Two-pane editor + autosave/publish API routes | **Done (2026-05-02)** — `blueprints/admin/cms/blueprint.py` extended with five ε routes: `GET /admin/edit/?id=<page_id>` (renders editor), `GET/PUT/DELETE /admin/api/draft/?id=<page_id>` (autosave CRUD), `POST /admin/api/publish/?id=<page_id>` (publish), `GET /admin/preview/?id=<page_id>` (δ-stubbed iframe target). All routes use `?id=<page_url>` query param to avoid leading-slash + path-converter awkwardness; matches the `page_id` key in `cms.db.drafts`. **Templates**: `templates/admin/cms/editor.html` two-pane layout — header (crumbs + title + kind badge + URL), toolbar (Save/Publish/Discard + ↗live), collapsible frontmatter `<textarea>`, body `<textarea>`, preview iframe. Inline JS shims: 2 s autosave debounce, 400 ms postMessage to preview (forward-compat for δ), explicit Save/Publish/Discard handlers, `beforeunload` guard for unsaved typing. Status bar reflects save state (`saving…` → `saved · HH:MM:SS`). **Preview stub**: `templates/admin/cms/preview_stub.html` shows the published page in an inner iframe with a banner explaining the δ gap; `postMessage({type:'cms-refresh'})` reloads the inner iframe (forward-compat). **Landing edit links**: `index.html` renders `edit ✎` next to every `lock=editable` leaf. **CodeMirror**: not vendored; editor falls back to `<textarea>` per W3-2 ratification. **End-to-end demo path**: `/admin/` → click `edit ✎` on a blog post → modify body → wait 2 s (autosave) → click Publish → `cms: publish /blog/<slug>/ [/blog/<slug>/]` commit appears in `git log`; round-trip is byte-preserving (verified 6/6 in α(md) phase). Conflict gate: `has_conflict()` returns 409 if on-disk source SHA-256 has shifted since `base_sha` capture. |
| CMS media routes — wave 2 item 8 closure (2026-05-02) | Wire `media.py` library into HTTP routes + editor modal | **Done (2026-05-02)** — Three routes in `blueprints/admin/cms/blueprint.py`: `POST /admin/api/media/` (multipart upload; field `file`; calls `media.upload`; 415 on disallowed mime; idempotent on bytes via SHA-256 dedupe), `GET /admin/api/media/?q=&mime=&limit=&offset=` (returns JSON list of `MediaRow`s with `url` + `thumb_url`), `PATCH /admin/api/media/<id>/` (alt_text + caption update). **Editor integration**: `templates/admin/cms/editor.html` gains a `Media…` toolbar button + `<dialog>` modal with drop-zone, file-picker, search box, thumbnail grid; clicking a thumbnail splices `![alt](url)` at the cursor and triggers autosave. Drag/drop upload supported; uploads also surface progress via the editor status bar. **Smoke (in-memory)**: SVG rejected by mime whitelist; PNG accepted (1×1 transparent fixture); content-addressed dedupe verified (same bytes → same `id`); `browse()` free-text + `image/*` glob filters work; `update_metadata()` direct write OK. Stray test artefact at `static/media/62/620c9d52...d38a.jpg` from pre-patch run — PI to remove manually (sandbox mount denied `rm`). **Surface remaining**: `media_rebuild_thumbnails` recovery command (deferred); media browser as standalone page (currently modal-only). |
| CMS ζ — settings UI (wave 4 item 9, 2026-05-02) | Build the 5-group settings surface + JSON exporter + theme.css regen + .env shadow rule | **Done (2026-05-02)** — `blueprints/admin/cms/settings.py` (239 lines): 5 groups (`site`, `nav`, `theme`, `contact`, `footer`) keyed as `group.<name>` rows in `cms.db.settings`; per-group `DEFAULTS`; `get_group` / `put_group` / `all_groups` round-trip JSON blobs (W4-1 ratified); `publish_settings()` atomically exports to `instance/site_settings.json` (W2-3 untracked) and regenerates `static/theme.css` (W4-2 ratified); `regenerate_theme_css(theme)` walks nested dicts and emits `:root { --<dotted-key>: <value>; … }`; `theme_css_cache_bust()` returns mtime for `?v=` querystring; `_SiteSettingsCache` class is mtime-keyed module-level cache for the request hook; `effective(key, group='contact', env_key=...)` enforces the shadow precedence `site_settings.<group>[key] ≻ os.environ`. **Routes** in `blueprint.py`: `GET /admin/settings/` (group selector + Publish All), `GET /admin/settings/<group>/` (JSON textarea editor with per-group help), `PUT /admin/api/settings/<group>/` (JSON blob save), `POST /admin/api/settings/publish/` (atomic export + theme regen + cache invalidate). `cms.edit` route now redirects SETTINGS-kind PageRefs to the form rather than the MD/YAML editor. **Templates**: `templates/admin/cms/settings/{index,group}.html` (zero deps; revert button; per-group help text). **app.py wiring**: `before_request` populates `g.site_settings` via `_SiteSettingsCache`; context processor extended with shadow-precedence resolution for `site_url`/`site_name`/`site_tagline`/`turnstile_site_key`/`contact_submit_url` (settings ≻ app.config); `theme_css_bust` injected for cache-busted `<link>` URLs in templates. **Smoke (in-memory)**: 5/5 groups default-load; round-trip put/get for site, nav, theme; publish writes 694 bytes JSON + 175 bytes CSS; theme.css emits clean `:root` block (note: keys with underscores like `font_stack` produce `--font_stack` not `--font-stack` — by design, PI renames keys to `font-stack` if hyphenated CSS vars are preferred); `effective()` shadow rule verified (env-default before publish; cms-override after). PI verification: `flask --app app run`, visit `/admin/settings/`, edit a group, click Save, click Publish All; then check `instance/site_settings.json` and `static/theme.css`. |
| CMS η — search & replace UI (wave 4 item 10, 2026-05-02) | Source-content search across editable surface + replace-via-drafts | **Done (2026-05-02)** — `blueprints/admin/cms/search.py` (228 lines): per-request walk over editable sources (W4-3 ratified: in-memory rebuild, no startup index); covers blog, methodology, davidsimmons slots, all 16 region problem YAMLs, NZ pattern files. `find_matches(query, whole_word=True, limit=500)` returns frozen `Match` dataclass (`page_id, path, line_no, line, before[≤2 lines], after[≤2 lines]`); `_build_pattern` wraps query in `\\b...\\b` when `whole_word` (W4-4 ratified default). `apply_replaces(find, replace, page_ids, whole_word)` — replace via drafts only (§10.5 invariant); per-page: `ref.projection.load` → `pat.subn` on body + frontmatter → `drafts.put_draft` with current `hash_concat` as `base_sha`; PROJECTED skipped (NotImplementedError catch — gated on α-rest). **Routes** in `blueprint.py`: `GET /admin/search/?q=&whole_word=` (find UI grouping by page) and `POST /admin/api/search/replace/` (JSON {find, replace, page_ids, whole_word}; returns `{drafts_created, results: {page_id: count}}`). **Template** `templates/admin/cms/search/index.html`: search input + whole-word toggle, grouped page-blocks with up to 5 matches each, mark-highlight on hit line, per-page checkboxes, replace bar with confirmation dialog and status feedback. **Smoke (live corpus)**: enumerate yields **657 editable sources** (5 blog + 1 methodology + 33 NZ pattern + 618 region leaves); `find("Aotearoa", whole_word=True)` returns 5 hits across 2 pages — verified contextually correct (blog post + methodology); empty/unknown queries return 0; `apply_replaces` correctly skips PROJECTED pages because their projections raise NotImplementedError. **Surface remaining**: replace works for DIRECT_MD + single-source HYBRID today; PROJECTED replace lights up after corpus_leaf_form_A ships. |
| CMS θ — diff / history / restore (wave 4 W3-3 closure, 2026-05-02) | Per-page git history + diff viewer + restore-to-draft | **Done (2026-05-02)** — `blueprints/admin/cms/history.py` (214 lines): three subprocess wrappers — `commits_for_page(page_id, limit=50)` runs `git log --pretty=%H%x09%h%x09%an<%ae>%x09%aI%x09%s -- <paths>` and returns frozen `Commit` dataclass list; `diff(page_id, from_sha, to_sha=None)` runs `git diff <rev> -- <paths>` with **root-commit fallback** (when `<sha>~` fails, falls back to the empty-tree SHA `4b825dc642cb6eb9a060e54bf8d69288fbee4904` so initial-commit diffs render correctly); `restore_to_draft(page_id, sha)` reads source content via `git show <sha>:<path>`, splits frontmatter via `_split_frontmatter`, calls `drafts.put_draft` with current on-disk `hash_concat` as `base_sha`. **Read-only over git** (§11.5 invariant — no `checkout`, `reset`, `revert`). `_is_safe_sha` validates sha as 4–40 hex chars before any subprocess call. **Restore is gated** for multi-source HYBRID and PROJECTED pages (raises `RestoreError` until α-rest ships). **Routes** in `blueprint.py`: `GET /admin/history/?id=<page_id>` (commit list, render via templates/admin/cms/history/index.html), `GET /admin/diff/?id=<page_id>&from=<sha>&to=<sha>?` (W4-5 ratified plain `<pre>` with light client-side colourisation: green +, red −, blue @@/+++/--- hunks), `POST /admin/api/restore/?id=<page_id>&sha=<sha>` (returns `{ok, edit_url}`). **Editor toolbar** gains a `History` button linking to the per-page commit list. **Smoke**: sha validator passes 6/6 boundary cases; `commits_for_page` returns 1 commit for an existing blog post; root-commit detection works (`git rev-parse --verify <sha>~` returns 128); empty-tree fallback emits valid `diff --git` + `+` lines via manual `git diff 4b825dc... <sha>`; invalid sha raises `RestoreError`. **v1 feature-complete** modulo the two preserved mastery surfaces (δ preview overlay, α-rest 7 stubbed projections). |
| CMS δ — preview overlay (2026-05-02) | Werkzeug context-local intercept for live draft preview | **Done (2026-05-02)** — `blueprints/admin/cms/overlay.py` (130 lines): frozen `Overlay` dataclass holding `intercepts: dict[Path, bytes]` (paths stored as resolved-absolute for robustness against symlinks / relative callers); `_active` `ContextVar[Overlay \| None]` with default None; `overlay_active(ov)` contextmanager using `set/reset` token pattern (correct teardown on success and exception); `install()` monkey-patches `Path.read_text` and `Path.read_bytes` once at app startup (idempotent — `_installed` flag); `uninstall()` for tests. **W3-1 simplified to candidate B narrowly**: A (Jinja loader subclass) was unnecessary because DIRECT_MD/HYBRID content is read by Python (`frontmatter.load`, `Path.read_text`) **not** through Jinja's loader; B's `Path.read_text` intercept is the universal mechanism. **`cms.preview` route**: resolves PageRef → loads draft → calls `projection.save` to materialise draft bytes → builds Overlay → activates contextmanager → dispatches internally via `current_app.test_client().get(page_id)` → returns response data verbatim to the iframe. **Falls through to stub** for PROJECTED pages and when no draft exists. PROJECTED draft preview requires the corpus render pipeline to be re-runnable from in-memory YAML (deeper render.py refactor — deferred). **Wired**: `_install_preview_overlay()` invoked at end of `create_app`. **Smoke**: ContextVar default=None; activation gives `_active.get() is ov`; restore returns to None on with-exit; `Path.read_text` returns disk content when no overlay, returns draft bytes when path in `intercepts`, restores after with-exit. |
| CMS α-corpus — corpus_leaf_form_A simple A (2026-05-02) | Problem-YAML byte-preserving projection (PI mastery surface lifted) | **Done (2026-05-02)** — `_ProblemYamlProjection` class added to `resolver.py`; `CORPUS_LEAF_FORM_A` and `PATTERN_FORM_A` instances now use it (was `_StubProjection`). **Simple A**: load reads only the Problem entity YAML at `source_paths[0]` (not transitive drivers/camps/claims/sources/methodologies); `EditorState.body` is the entire YAML text, `frontmatter=None`. Save writes the YAML back byte-preservingly with trailing-newline normalisation (no PyYAML reserialise — same trick as `_FrontmatterMdProjection` to dodge key-reorder / quote-strip / list-style flips). Transitive entities still edited via DASHBOARD-SPEC at `/admin/yaml/<region>/<entity_type>/<entity_id>/`. **Round-trip verification**: 46/46 Auckland problem YAMLs byte-identical; 5/5 Wellington problems byte-identical (cross-region spot check). **Editor flow**: `/admin/edit/?id=/research/<region>/<theme>/<slug>/` now opens the typed Problem YAML in the textarea; PI edits, autosaves at 2 s, publishes via the existing pipeline (which calls `projection.save` → atomic write → git commit). **PROJECTED preview still falls through to stub** because the render pipeline (`content/<region>/tools/render.py`) needs a refactor to accept in-memory YAML rather than re-reading from disk; deferred. v2 strategy-B (round-trip MD ↔ YAML graph parser, $R(R^{-1}(m'))=m'$) remains the genuine v2 stretch. |
| CMS v1 disk repair (2026-05-02) | Repair 7 Write-tool-truncated/NUL-tailed files from prior session | **Done (2026-05-02)** — `blueprints/admin/save_pipeline.py` (truncated at 265), `blueprints/admin/cms/resolver.py` (truncated at 347 — reconstructed 321 lines), `blueprints/admin/cms/publish.py` (4175 NUL-byte tail → stripped to 234 lines), `blueprints/admin/cms/history.py` (2383 NUL-byte tail → stripped to 157 lines), `blueprints/admin/cms/settings.py` (truncated at 235 — appended tail + v2-4 hooks), `blueprints/admin/cms/blueprint.py` (truncated at 28 — reconstructed ~360 lines), `app.py` (truncated at 124 — appended tail to 164 lines). All repaired via Python heredoc through bash sandbox. Detection: `wc -l` vs Read-tool mismatch; `tail -3 | cat -A` shows truncation or NUL bytes. |
| CMS v2 tier 2 admin shell (2026-05-02) | Polish `/admin/` with classic-preset aesthetic; 8 surface templates; 29 routes | **Done (2026-05-02)** — Dark sidebar + breadcrumbs + tab bar; Inter + Fraunces fonts; paper-warm `#fbf8f0` canvas; copper accent `#c4956a`. Surfaces: landing, editor (Body/Frontmatter/SEO/History tabs + viewport switcher), theme customizer, media browser, settings (5 groups), search & replace, diff/history. All 29 admin routes HTTP 200 on Flask test client. Asset deliverables: `content/_themes/{defaults,classic,editorial,documentary}.yaml`, `content/_theme/{header,footer}.md`, `content/_blocks/registry.yaml`, `content/_schema/seo.schema.json`, `tests/cms/fixtures/blocks/{01..08}_*.md`, `templates/admin/cms/_shell.html`. |
| CMS v2 wave bodies (v2-4/v2-5/v2-6/v2-7/v2-8) shipped (2026-05-02/03) | Pillow pipeline, theme composer, slot render, SEO splice, nav builder | **Done (2026-05-02/03)** — **(v2-4)** `theme.compose` (right-biased deep merge, empty-as-inherit), `derive_spacing_scale` (Fibonacci φ), `get/set_active_preset` (cms.db `theme_preset` key); `regenerate_theme_css` extended to compose defaults ⊕ preset ⊕ override. **(v2-5)** `media_transforms` DDL added to `db.py`; `crop_transform` (fractional Pillow crop + atomic INSERT), `focal_set/get` (singleton metadata upsert), `resize_lazy` (LANCZOS thumbnail, idempotent), `srcset_for` (all 4 canonical widths; canonical-width descriptors). **(v2-6)** `slot_render` (mtime-based cache fall-through), `regenerate_slot_cache` (atomic os.replace); resolver `/_theme/<slot>/` patterns; `content/_theme/{header,footer}.md` seeded. **(v2-7)** `docs/CMS-IMPL-WAVE-v2-7.md` authored; `templates/admin/cms/settings/nav.html` drag-reorder UI with SortableJS (CDN); depth ≤ 3 enforced; blueprint routes to nav template for group='nav'. **(v2-8)** `SeoView.from_frontmatter`, `jsonld`, `splice_seo_into_frontmatter` (locality-preserving: only `seo:` block replaced verbatim). `tests/cms/test_v2_bodies.py`: 32 tests, all green. v2-1 parser preserved as PI mastery (CMS-SPEC-v2 §14 row 1). |
| CMS v2-1 parser/serializer (2026-05-03) | Block-directive round-trip: `parse()` + `serialize()` in `blueprints/admin/cms/blocks/`; 12/12 round-trip + locality tests green; `test_blog_corpus_is_plain_md` confirms no false positives on v1 content. |
| CMS v2-2 visual editor (2026-05-03) | Block parse/serialize API routes (`POST /admin/api/blocks/parse/`, `POST /admin/api/blocks/serialize/`); visual editor toggle in editor.html — Visual mode parses body → colored block cards with inline attr editing; Source mode serializes back (dirty blocks → canonical form, clean → verbatim). 44/44 tests green. |
| CMS v2-3 block library + renderer wire-up (2026-05-03) | `blueprints/admin/cms/blocks/renderer.py` — `render_md_with_blocks(body, jinja_env)`; 12 Jinja block templates in `templates/blocks/`; `static/blocks.css` (178 lines); wired into `blueprints/blog.py` (`_load_post` calls `render_md_with_blocks`); `render_md_with_blocks` registered as Jinja global in `app.py`. Fast path for all-RawSpan trees preserves v1 output byte-for-byte. Smoke: blog index + 3 post routes HTTP 200; `::callout{type="warning"}` renders correct `<aside>`; v1 round-trip `render == markdown.convert` ✓. |
| CMS v2-9 page-builder section blocks (2026-05-03) | 3 full-bleed section templates (`section_hero`, `section_feature_grid`, `section_cta`) + CSS `width:100vw; margin-left:calc(50%-50vw)` full-bleed trick; registry entries already present; hyphen→underscore normalization in renderer template lookup. Smoke: all 3 section wrappers render; v1 round-trip preserved. |
| CMS v2-10 featured image + excerpt + tag archive + analytics + mobile preview (2026-05-03) | **Featured image + excerpt**: `_load_post` extracts `featured_image` dict (src/alt/focal) and `excerpt` (falls back to summary → first paragraph); tags coerced to `str` (YAML year-tags were parsed as int). *