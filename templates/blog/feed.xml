<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ site_name }} — Blog</title>
  <subtitle>{{ site_tagline }}</subtitle>
  <link href="{{ site_url }}{{ url_for('blog.feed') }}" rel="self" />
  <link href="{{ site_url }}{{ url_for('blog.index') }}" />
  <id>{{ site_url }}{{ url_for('blog.index') }}</id>
  <updated>{{ last_updated.isoformat() }}T00:00:00Z</updated>
  {%- for post in posts %}
  <entry>
    <title>{{ post.title|e }}</title>
    <link href="{{ site_url }}{{ url_for('blog.post', slug=post.slug) }}" />
    <id>{{ site_url }}{{ url_for('blog.post', slug=post.slug) }}</id>
    <updated>{{ post.date.isoformat() }}T00:00:00Z</updated>
    <published>{{ post.date.isoformat() }}T00:00:00Z</published>
    <author><name>{% if post.author == 'agent' %}Weekly agent{% else %}Luke Simmons{% endif %}</name></author>
    {%- if post.summary %}
    <summary>{{ post.summary|e }}</summary>
    {%- endif %}
    <content type="html">{{ post.body_html|e }}</content>
    {%- for tag in post.tags %}
    <category term="{{ tag|e }}" />
    {%- endfor %}
  </entry>
  {%- endfor %}
</feed>
