# Auckland congestion

A short paragraph that's plain Markdown — no block syntax needed. The
parser MUST emit a single RawSpan covering this entire body, and
serialize MUST reproduce it byte-identically.

A second paragraph with **bold**, _italic_, and a [link](https://example.com).

- A list item
- Another list item

A trailing paragraph after the list.
